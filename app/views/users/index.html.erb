<h1>Me & You Tube</h1>
<h2>What should we watch together?<h2>

<% @users.each do |user| %>
	<%= image_tag user.image %>
	<%= user.name %>
<% end %>

<% @users.each do |user| %>
 <% user.movies.each do |movie| %>
		<p><%= movie.title %></p>
		<p><%= image_tag movie.thumbnail %></p>
		<p><%= movie.description %></p>
		<p><%= movie.url %></p>
	<% end %>
<% end %>